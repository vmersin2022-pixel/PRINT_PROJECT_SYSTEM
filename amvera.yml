meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands:
    - npm install
    - npm run build
  artifacts:
    dist: dist

run:
  command: npx serve -s dist -l 3000
  containerPort: 3000
