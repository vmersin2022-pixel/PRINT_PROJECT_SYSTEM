
meta:
  environment: node
  toolchain:
    name: browser
    version: 20

build:
  # Amvera сама сделает npm install && npm run build
  # Vite собирает проект в папку dist
  artifacts:
    "dist/*": /
